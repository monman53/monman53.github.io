# AGENT.md

## ブランチ運用

- 作業は `dev` ブランチで行う
- `main` ブランチへのマージは明示的に指示された場合のみ行う
- `main` へのマージは必ず新しいマージコミットを作成する（fast-forward 禁止）
- マージは明示的に指示された際にエージェントが実施する
- `push` は手動で行う（エージェントは実施しない）

## ドキュメント管理

- 機能追加・変更を行った場合は `README.md` および `AGENT.md` を適宜更新する
